# Gym Progress PWA v1.6.4 — 2026-09-05

- Empty-device sync is treated as restore, not a schedule/programme conflict.
- Conflict choices use aligned label-left / radio-right rows.
- Successful login refreshes the global account/header state immediately.
- Friends `+ Add friend` is isolated on its own right-aligned action row, and the Home `Gym Progress` title stays on one line.
- Final exercise status points to Stretching next.
- Share checkmark is neutral when unselected and green only when selected.
- Includes the v1.6.3 record-level sync convergence, Tracker live-update and upsert dedupe fixes.
- No database migration is required.
